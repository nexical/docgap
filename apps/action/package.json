{
    "name": "@docgap/action",
    "private": true,
    "version": "0.1.0",
    "main": "dist/index.js",
    "scripts": {
        "build": "ncc build src/index.ts -o dist --license licenses.txt && cp $(find ../../node_modules -name tiktoken_bg.wasm | grep -v lite | head -n 1) dist/ && sed -i 's/loadLanguage_require(\"url\")/require(\"url\")/g' dist/index.js && sed -i \"s/runtime: 'worker_threads'/runtime: 'child_process'/g\" dist/index.js && mkdir -p dist/entry && cp $(find ../../node_modules -name process.js | grep tinypool | head -n 1) dist/entry/process.js",
        "dev": "ncc build src/index.ts -o dist -w",
        "test:unit": "vitest run tests/unit --coverage",
        "test:integration": "vitest run tests/integration"
    },
    "dependencies": {
        "@actions/core": "^1.10.1",
        "@actions/github": "^6.0.0",
        "@docgap/core": "workspace:*"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.1",
        "typescript": "^5.0.0"
    }
}